import { CMC_API_KEY, CMC_TERNOA_TOKEN_ID, CMC_URL, KEYRING_TYPE, WS_PROVIDER } from '@env'

export const CONFIG = {
  cmcTokenId: CMC_TERNOA_TOKEN_ID,
  wsProvider: WS_PROVIDER,
  cmcApiKey: CMC_API_KEY,
  cmcUrl: CMC_URL,
  keyRingType: KEYRING_TYPE,
}
